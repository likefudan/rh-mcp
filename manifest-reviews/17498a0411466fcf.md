# Reviewed provider tool-set expansion

Two consecutive owner-assisted `rh-mcp admin discover` runs on 2026-08-30
returned byte-equivalent tool payloads after removing only `observed_at`.
Discovery enumerated schemas and invoked no provider tool.

- Previous tool count: 55
- Observed tool count: 59
- Appeared: 4
- Disappeared: 0
- Stable candidate SHA-256:
  `2868efc08fbff012be5dfec658787422e0d9607345cd05f2f75df1da5a722b8e`
- Provider-surface digest:
  `sha256:17498a0411466fcfb475e0cfb160f145df38702c2aee13224b0b3ae5340b1e96`

The four appeared tools are `get_sec_filing_index`, `get_sec_filing`,
`get_sec_filing_facts`, and `get_sec_filing_facts_catalog`. Their names,
descriptions, complete input/output schemas, annotations, and mutation blast
radii were reviewed. All four are reads of public SEC filing data, accept no
account, order, cash, position, watchlist, or scan input, and change no provider
state. They are explicitly `denied` / `mutates: false`: no current consumer
requires them, and drift recovery must not silently widen permission.
`readOnlyHint: true` was evidence, never authority. Provider-returned filing text, facts,
identifiers, concepts, sections, cursors, and offsets remain untrusted data and
grant no authority to invoke another capability.

All prior 55 entries are byte-equivalent in every provider-derived field and
retain their reviewer decisions verbatim. The resulting manifest keeps 36
allowed reads and 11 allowed non-trading mutations; its denied set is the same
8 trading/simulation tools plus these 4 known SEC reads. This record does not
itself authorize merge, tag, or release; the exact
source and artifacts still require independent review under DESIGN §12.4.
